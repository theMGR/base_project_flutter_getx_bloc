import 'package:get/get.dart';

import 'lead_list_controller.dart';

class LeadListBinding extends Bindings {
  @override
  void dependencies() {
    // already used in home binding
    //Get.lazyPut(() => LeadListController());
  }
}
